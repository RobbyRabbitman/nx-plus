{
  "name": "tools-local-registry",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "tags": ["scope:node", "type:tool"],
  "targets": {
    "serve": {
      "executor": "@nx/js:verdaccio",
      "options": {
        "port": 4321,
        "config": "{projectRoot}/src/config.yml",
        "storage": "{projectRoot}/storage"
      }
    },
    "publish": {
      "cache": true,
      "inputs": ["publishable"],
      "outputs": ["{options.storage}"],
      "command": "ts-node {projectRoot}/src/publish.cli.ts",
      "options": {
        "storage": "{workspaceRoot}/{projectRoot}/storage"
      }
    },
    "test": {
      "cache": true,
      "inputs": ["publishable"],
      "dependsOn": ["publish"]
    },
    "e2e-version-matrix-vite": {
      "command": "ts-node {projectRoot}/src/e2e-version-matrix-vite.cli.ts"
    }
  }
}
