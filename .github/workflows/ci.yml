name: ci

on:
  pull_request:

concurrency:
  group: ${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  main:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # TODO: why are the node_modules not mounted and reinstalled when the dev container is started?
      - run: corepack enable
      - uses: actions/setup-node@v4
        with:
          node-version-file: package.json
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - uses: nrwl/nx-set-shas@v4

      - run: sh .devcontainer/exec-cmd.sh echo '🚀 devcontainer started 🚀'

      - run: sh .devcontainer/exec-cmd.sh pnpm nx report

      - run: sh .devcontainer/exec-cmd.sh sh -c 'echo "NX_BASE=$NX_BASE NX_HEAD=$NX_HEAD"'

      # - run: pnpm nx run tools-commitlint:exec:message --value "${{ github.event.pull_request.title }}"

      # - run: |
      #     pnpm nx format:check
      #     pnpm nx affected -t lint-eslint test test-vite build e2e-vite-version-matrix --configuration ci
      #   env:
      #     NX_VERBOSE_LOGGING: ${{ runner.debug && true || false }}
